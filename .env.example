# Copy to .env and fill in. .env is gitignored.

# --- Server ---
PORT=4318
HOST=0.0.0.0
DATABASE_PATH=.data/claude-usage.sqlite

# Plan limits source: "fetch" reads the local OAuth token; "push" serves a
# snapshot sent by the limits pusher (used for remote deployment).
LIMITS_SOURCE=fetch

# --- Automatic Claude refresh nudge (fetch mode and limits pusher) ---
# Runs a small local Claude prompt one minute after each observed 5h reset.
# Set to 0 to disable.
CLAUDE_REFRESH_NUDGE=1
CLAUDE_REFRESH_OFFSET_MS=60000
CLAUDE_REFRESH_TIMEOUT_MS=120000
CLAUDE_REFRESH_RETRY_MS=60000
# Use an absolute path here if your service manager cannot resolve "claude".
CLAUDE_REFRESH_CLI=claude
CLAUDE_REFRESH_PROMPT=Reply only with: refreshed
CLAUDE_REFRESH_MAX_BUDGET_USD=0.05

# --- Telegram session-refresh alerts (fetch mode) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Web Push phone notifications (push mode) ---
# Generate once: npx web-push generate-vapid-keys
# Push features activate only when all three are set.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
# Hour (0-23, server local time) to push a daily cost/token/session summary.
# Leave empty to disable.
DAILY_SUMMARY_HOUR=

# --- Limits pusher (runs on your local machine, not the remote server) ---
DASHBOARD_INGEST_URL=
# Optional Authorization header value for the remote reverse proxy.
DASHBOARD_INGEST_AUTH=
LIMITS_PUSH_INTERVAL_MS=60000
